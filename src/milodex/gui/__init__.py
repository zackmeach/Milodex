"""Milodex GUI subsystem (PySide6 + Qt Quick).

Phase 5 GUI implementation per ADR 0033 and ADR 0035. The package
contains bundled fonts (assets/fonts/), the font loader (fonts.py),
and — in subsequent PRs — the QML Theme infrastructure, foundational
components, and application shell.
"""
