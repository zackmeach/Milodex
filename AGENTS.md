# Milodex — Agent Guide

This project keeps a single canonical agent/contributor guide: **[`CLAUDE.md`](./CLAUDE.md)**.

Read `CLAUDE.md` for architecture, key design rules (the risk layer, the
autonomy boundary, the promotion pipeline), config schema, environment setup,
code style, and gotchas. When in doubt about tone, scope, or tradeoffs, defer
to `docs/FOUNDER_INTENT.md` as described there.

This file intentionally contains no duplicated guidance — a second copy only
drifts out of sync. Update `CLAUDE.md` instead.
